# Copy this file to keystore.properties (never commit keystore.properties)
# storeFile path is relative to the cacree/ root directory
storeFile=../cacree-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=cacree
keyPassword=YOUR_KEY_PASSWORD
